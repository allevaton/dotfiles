# Personal Preferences

## Pull Requests
- Keep PR descriptions concise and plain — no `## Summary` / `## Test plan` section headers. Just describe the change directly.
- Use imperative mood for PR titles and commit messages (e.g. "Add feature" not "Adds feature").
